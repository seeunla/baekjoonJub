-- 코드를 작성해주세요
SELECT COUNT(*) AS FISH_COUNT, MAX(IF(LENGTH is null, 10, LENGTH)) AS MAX_LENGTH, FISH_TYPE
FROM FISH_INFO
GROUP BY FISH_TYPE
HAVING AVG(IF(LENGTH is null, 10, LENGTH)) >= 33
ORDER BY FISH_TYPE